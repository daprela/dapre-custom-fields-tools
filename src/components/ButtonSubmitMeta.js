// eslint-disable-next-line react/prop-types
const ButtonSubmitMeta = ({ className, name }) => (
  <input
    className="js-submitOptions c-metaSubmitButton button button-primary"
    type="submit"
    name="submit_options"
    value="Read/Write Values"
  />
);

export default ButtonSubmitMeta;
